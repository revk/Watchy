// Faces list
// Add new faces in face_whatever.c, and add that to CMakeList.txt, inlcude "face.h" in your code
// Your C code can include many faces, list them here

#ifndef	face
#define	face(name,description)
#endif

// List of faces
face(basic,Basic digital)
face(analogue,Analogue)

#undef face
